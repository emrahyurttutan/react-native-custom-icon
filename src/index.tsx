import IconMoon from './Iconmoon';
import BaseIconNames from './iconNames';
import * as Types from './types';
export const iconNames = BaseIconNames;
export default IconMoon;
export interface IconMoonProps extends Types.IconMoonProps {}
